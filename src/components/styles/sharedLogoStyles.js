import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      alignItems: 'center',
      marginBottom: 25,
    },
    logo: {
      width: 200,
      height: 50,
    },
    containerLogo: {
      justifyContent: 'center',
      flexDirection: 'row',
      alignItems: 'center',
    },
    back: {
      right: 50,
    },
    header: {
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: "center",
      marginTop: 25,
    },
  });