import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      alignItems: 'center',
    },
    logo: {
      width: 200,
      height: 50,
    },
    containerLogo: {
      justifyContent: 'center',
      flexDirection: 'row',
      alignItems: 'center',
    },
    back: {
      right: 50,
    },
    header: {
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: "center",
      position: 'absolute',
      top: 50,
    },
  });